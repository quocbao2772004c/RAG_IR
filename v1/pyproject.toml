[project]
name = "rag-ir-v1"
version = "0.1.0"
description = "Student RAG server v1 using TF-IDF retrieval."
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
    "python-dotenv",
    "openai",
    "numpy",
    "httpx",
]

[tool.uv]
package = false
